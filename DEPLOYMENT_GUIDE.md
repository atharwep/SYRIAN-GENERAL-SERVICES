# 🚀 دليل التشغيل والنشر الشامل (Depolyment Guide)

هذا الدليل يشرح كيفية تشغيل الموقع على جهازك، وكيفية رفعه (نشره) على الإنترنت ليكون متاحاً للجميع.

---

## 🖥️ أولاً: التشغيل المحلي (على جهازك)

لتشغيل الموقع أثناء التطوير، تحتاج إلى فتح نافذتين "موجه أوامر" (Terminal).

### 1️⃣ تشغيل قاعدة البيانات والباك اند (Backend)
1. افتح الـ Terminal وانتقل لمجلد الباك اند:
   ```bash
   cd apps/backend
   ```
2. تأكد من أنك قمت بإعداد ملف `.env` (راجع ملف `NEXT_STEPS.md`).
3. شغل الأمر التالي لبدء السيرفر:
   ```bash
   npm run start:dev
   ```
   *سيعمل السيرفر على الرابط: `http://localhost:3001`*

### 2️⃣ تشغيل الواجهة الأمامية (Frontend)
1. افتح نافذة Terminal ثانية وانتقل لمجلد الفرونت اند:
   ```bash
   cd apps/frontend
   ```
2. شغل الأمر التالي:
   ```bash
   npm run dev
   ```
   *سيعمل الموقع على الرابط: `http://localhost:3000`*

---

## 🌐 ثانياً: نشر الموقع على الإنترنت (Deployment)

للحصول على أفضل أداء وأقل تكلفة، ننصح باستخدام التقسيم التالي عند الرفع:

### 1. قاعدة البيانات (Database)
استخدم خدمة سحابية مجانية/رخيصة لـ PostgreSQL مثل:
*   **Neon.tech** (موصى به جداً وسريع)
*   **Supabase**
*   **Railway Database**

**الخطوات:**
1. أنشئ حساباً في إحدى هذه الخدمات.
2. أنشئ مشروعاً جديداً واحصل على رابط الاتصال `DATABASE_URL`.
3. ضعه في ملف `.env` في المشروعين (Frontend & Backend).

### 2. الواجهة الخلفية (Backend - NestJS)
أفضل منصات لرفع تطبيقات NestJS:
*   **Render.com** (خيار مجاني ممتاز)
*   **Railway.app** (سهل جداً)
*   **Fly.io**

**خطوات الرفع على Render:**
1. ارفع كود المشروع على GitHub.
2. سجل في Render واربط حساب GitHub.
3. أنشئ "New Web Service".
4. اختر المستودع (Repo).
5. في إعدادات **Root Directory** اكتب: `apps/backend`.
6. في **Build Command** اكتب: `npm install && npx prisma generate && npm run build`.
7. في **Start Command** اكتب: `npm run start:prod`.
8. أضف الـ Environment Variables (مثل `DATABASE_URL` و `JWT_SECRET`).

### 3. الواجهة الأمامية (Frontend - Next.js)
أفضل منصة على الإطلاق لـ Next.js هي **Vercel** (الشركة المطورة لـ Next.js).

**خطوات الرفع على Vercel:**
1. ارفع كود المشروع على GitHub (نفس المستودع السابق).
2. سجل في Vercel.com.
3. اضغط "Add New Project" واختر المستودع.
4. في إعدادات **Root Directory** (اضغط Edit) واختر `apps/frontend`.
5. أضف المتغيرات في Environment Variables.
6. اضغط **Deploy**.

---

## ✅ ملخص الروابط بعد النشر

*   **Frontend:** `https://your-project.vercel.app` (الرابط الذي ستعطيه للمستخدمين).
*   **Backend:** `https://your-project.onrender.com` (الرابط الذي يستخدمه الفرونت اند للاتصال).
*   **Database:** (رابط سري مخفي في السيرفرات).

---

## 💡 نصيحة هامة
تأكد من تحديث رابط الـ API في الفرونت اند عند النشر.
بدلاً من `http://localhost:3001`، يجب أن يصبح رابط الباك اند الجديد (مثلاً `https://api.your-site.com`).
يمكنك فعل ذلك عبر المتغير `NEXT_PUBLIC_API_URL` في ملف `.env` الخاص بالفرونت اند.
