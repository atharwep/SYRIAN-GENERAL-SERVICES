# 🌐 خطوات ربط الموقع بقاعدة بيانات MySQL على الإنترنت

لقد قمت بتجهيز ملفات النظام ليعمل مع قاعدة بيانات حقيقية (MySQL) بدلاً من التخزين المحلي. اتبع الخطوات التالية للتفعيل:

## 1️⃣ إنشاء قاعدة البيانات (cPanel أو أي استضافة)

1. ادخل إلى لوحة تحكم الاستضافة الخاصة بك.
2. اذهب إلى **MySQL Databases**.
3. أنشئ قاعدة بيانات جديدة باسم: `muamalati_db`.
4. أنشئ مستخدم جديد لقاعدة البيانات (User) وكلمة مرور.
5. اربط المستخدم بالقاعدة وأعطه جميع الصلاحيات (All Privileges).

## 2️⃣ استيراد الجداول (phpMyAdmin)

1. افتح **phpMyAdmin** من لوحة التحكم.
2. اختر قاعدة البيانات التي أنشأتها `muamalati_db`.
3. اضغط على تبويب **Import**.
4. اختر الملف المرفق في المشروع: `database.sql`.
5. اضغط **Go**. سيتم إنشاء جميع الجداول (المرضى، السجلات، المستخدمين..).

## 3️⃣ تشغيل السيرفر (Node.js)

هذا الموقع يحتاج لبيئة Node.js للعمل مع قاعدة البيانات.

1. تأكد من تثبيت Node.js على جهازك أو السيرفر.
2. افتح مجلد المشروع في التيرمينال واكتب:
   ```bash
   npm install
   ```
3. أنشئ ملف جديد بجانب `server.js` اسمه `.env` وضع فيه بيانات الاتصال الخاصة بك:
   ```env
   DB_HOST=localhost
   DB_USER=اسم_مستخدم_القاعدة
   DB_PASSWORD=كلمة_المرور
   DB_NAME=muamalati_db
   PORT=3001
   ```
4. شغل السيرفر بالأمر:
   ```bash
   node server.js
   ```

## 4️⃣ ربط الواجهة الأمامية (Frontend)

1. افتح ملف `html-platform/js/config.js`.
2. تأكد أن الرابط يشير للسيرفر الخاص بك:
   ```javascript
   const CONFIG = {
       API_BASE_URL: "http://your-website.com:3001", // أو localhost إذا كنت تجرب محلياً
       ...
   };
   ```

✅ **النتيجة:**
الآن عند تسجيل الدخول أو إضافة سجل طبي، سيتم حفظ البيانات **فعلياً** في قاعدة البيانات MySQL التي أنشأتها، ولن تضيع عند تحديث الصفحة أو تغيير الجهاز.
