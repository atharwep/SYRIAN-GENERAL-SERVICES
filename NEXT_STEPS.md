# 🚀 تشغيل النظام

## 1. نقل ملفات الـ HTML القديمة
تم إنشاء مجلد **`legacy-html`** وتم وضع ملفات الـ HTML القديمة بداخله.
(في حال لم تكن قد فعلت ذلك يدوياً، قم بإنشاء المجلد `legacy-html` وانقل مجلد `html-platform` بداخله).

## 2. نقل ملف الـ Backend القديم
تم تغيير اسم المجلد **`backend`** إلى **`legacy-backend`**.
(في حال لم تكن قد فعلت ذلك يدوياً، قم بتغيير اسم مجلد `backend` الحالي إلى `legacy-backend` لأرشفته).

## 3. نقل مجلد الـ Frontend
مجلد **`frontend`** الذي تم تحديثه الآن يجب نقله إلى **`apps/frontend`**.
(قم بسحب وإفلات مجلد `frontend` من المجلد الرئيسي إلى داخل مجلد `apps`).

## 4. تشغيل المشروع
بعد ترتيب الملفات كما هو موضح أعلاه، نفذ الأوامر التالية:

### تشغيل الواجهة الخلفية (Backend)
```bash
cd apps/backend
npm install
npx prisma generate
npm run start:dev
```

### تشغيل الواجهة الأمامية (Frontend)
```bash
cd ../frontend
npm install
npm run dev
```

إذا قمت بهذه الخطوات البسيطة، سيعمل المشروع بنجاح!
