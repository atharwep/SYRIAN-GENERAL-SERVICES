# 🌐 الحل الذكي: قاعدة بيانات Firebase (بدون استضافة / مجانية)

بما أنك لا تملك استضافة مدفوعة (Hosting)، فالحل الأمثل هو استخدام **Firebase Realtime Database** من Google. 
هذه الخدمة:
1.  **مجانية** تماماً للتطبيقات المتوسطة.
2.  **لا تحتاج لسيرفر** (Serverless).
3.  **سريعة جداً** (Realtime).
4.  الموقع مجهز مسبقاً للعمل معها.

عليك فقط اتباع الخطوات البسيطة التالية للحصول على "المفتاح" الخاص بك وتشغيل قاعدة البيانات:

---

## 🚀 خطوة 1: إنشاء مشروع مجاني

1.  اذهب إلى [Google Firebase Console](https://console.firebase.google.com/).
2.  اضغط على **Create a project**.
3.  سمّه مثلاً `muamalati-app` واضغط Continue.
4.  عطل Google Analytics (للسرعة) واضغط **Create Project**.

---

## ⚡ خطوة 2: إنشاء قاعدة البيانات

1.  من القائمة الجانبية اليسرى، اختر **Build** ثم **Realtime Database**.
2.  اضغط **Create Database**.
3.  اختر الموقع (Location) - اترك الافتراضي (United States).
4.  في إعدادات الأمان (Security Rules)، اختر **Start in test mode** (للبداية فقط) ثم اضغط **Enable**.

---

## 🔑 خطوة 3: الحصول على مفاتيح الربط

1.  اضغط على أيقونة الإعدادات ⚙️ (بجانب Project Overview في الأعلى) واختر **Project settings**.
2.  انزلق لأسفل الصفحة حتى قسم **Your apps**.
3.  اضغط على أيقونة الويب `</>`.
4.  اكتب اسم التطبيق `Muamalati Web` واضغط **Register app**.
5.  سيظهر لك كود طويل يحتوي على `apiKey`, `authDomain`, `databaseURL`... إلخ.
6.  **انسخ هذا الكود**.

---

## 🔗 خطوة 4: وضع المفاتيح في موقعك

1.  افتح الملف في مشروعك:
    `html-platform/js/firebase-config.js`
2.  استبدل القيم الموجودة في الأعلى بالقيم التي نسختها من جوجل، لتصبح هكذا تقريباً:

```javascript
const firebaseConfig = {
    apiKey: "AIzaSyB...",  // القيم الحقيقية من جوجل
    authDomain: "muamalati-app.firebaseapp.com",
    projectId: "muamalati-app",
    storageBucket: "muamalati-app.appspot.com",
    messagingSenderId: "123456789",
    appId: "1:123456789:web:abcdef",
    databaseURL: "https://muamalati-app-default-rtdb.firebaseio.com"
};
```

---

✅ **مبروك!**
الآن عند فتح الموقع وتسجيل الدخول أو إضافة سجل طبي، سيقوم الموقع تلقائياً بالحفظ في **قاعدة بيانات جوجل السحابية** بدلاً من جهازك فقط. يمكنك فتح الموقع من الهاتف والكمبيوتر وستجد نفس البيانات.
