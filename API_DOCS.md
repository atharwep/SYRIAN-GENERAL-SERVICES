# توثيق واجهة البرمجية (API Documentation) - منصة وصول

## 🔐 نظام الحسابات (Auth)
- `POST /api/auth/register` - تسجيل مستخدم جديد.
- `POST /api/auth/login` - تسجيل الدخول والحصول على JWT.

## 🩺 نظام الأطباء (Doctors)
- `GET /api/doctors` - قائمة جميع الأطباء المتاحين.
- `POST /api/doctors/book` - حجز موعد جديد.
- `POST /api/doctors/report` - إصدار تقرير طبي PDF.

## 🚕 نظام التكسي (Taxi)
- `GET /api/taxi/available` - جلب السائقين المتاحين حالياً.
- `POST /api/taxi/request` - طلب رحلة جديدة وتخمين السعر.

## 💳 المحفظة والخدمات (Wallet & Services)
- `GET /api/wallet/balance/:userId` - معرفة الرصيد الحالي.
- `POST /api/wallet/topup` - شحن رصيد المحفظة.
- `POST /api/wallet/pay-bill` - دفع فواتير سيريتل، MTN، والإنترنت.

---
جميع الطلبات تتطلب ترويسة أمان: `Authorization: <token>`
